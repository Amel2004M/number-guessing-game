# Number Guessing Game

Simple command-line number guessing game written in Python.

## Requirements

- Python 3.8+

## Running

From the project root run:

```bash
python app.py
```

Follow the on-screen prompts to play.

## Project Files

- `app.py` — main application.

## Next steps

- Add tests
- Add a license
- Package with `pyproject.toml` or `requirements.txt` if dependencies are added
